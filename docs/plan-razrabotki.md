# План разработки HobbyLife для ИИ-агентов

**Как пользоваться:** задачи выдаются агентам строго по порядку (зависимости указаны). Каждой задаче агенту передаётся: (1) текст карточки задачи целиком, (2) указанные в карточке разделы ТЗ (`tz-internet-magazin.md`), (3) актуальный код репозитория. Задача считается принятой только при выполнении всех пунктов Definition of Done (DoD). Один агент — одна задача — одна ветка — один PR.

**Общие правила для всех задач (включать в каждый промпт агенту):**

- Монорепозиторий: `backend/`, `frontend/`, `docker-compose.yml`, `docs/`.
- Backend: Python 3.12, FastAPI, SQLAlchemy 2.0 async, Pydantic v2. Стиль: ruff (line-length 100), mypy для сервисного слоя. Роутеры не содержат бизнес-логики и не обращаются к БД напрямую — только сервисы.
- Frontend: Next.js App Router, TypeScript strict, Tailwind. Никаких ценовых вычислений на фронте.
- Деньги: `Decimal` / `NUMERIC(12,2)`. Валюта — KGS, формат `1 250 сом`.
- Каждая задача с изменением схемы БД включает Alembic-миграцию (upgrade + downgrade, проверенные с нуля).
- Каждая задача включает тесты на новую функциональность; существующие тесты не должны падать.
- Не выдумывать API платёжных провайдеров: до получения реальных доступов используется только MockPaymentProvider.
- Секреты и настройки — только через ENV (pydantic-settings); в репозитории — `.env.example`.
- Каждый PR обновляет `docs/CHANGELOG.md` (1–3 строки) и, при изменении API, автогенерируемую OpenAPI-спеку не редактирует руками.

---

## Фаза 0. Каркас (задачи 0.1–0.3, последовательно)

### Задача 0.1 — Инфраструктура репозитория
**Контекст из ТЗ:** разделы 2, 3.
**Сделать:** структуру монорепо; `docker-compose.yml` с сервисами postgres:16, redis:7, minio (S3 для разработки), api (пустой FastAPI c `GET /health`), frontend (create-next-app, страница-заглушка), caddy (проксирует `/` → frontend, `/api` → api); Dockerfile'ы (multi-stage); `.env.example` со всеми переменными; Makefile (`make up`, `make test`, `make lint`, `make migrate`).
**DoD:** `docker compose up` с нуля поднимает всё; `curl localhost/api/health` → `{"status":"ok"}`; фронт открывается; README с инструкцией запуска.

### Задача 0.2 — Скелет backend-приложения
**Контекст из ТЗ:** 2.1, 3.1.
**Сделать:** структуру `app/` по ТЗ; `config.py` (pydantic-settings); `database.py` (async engine, session, `get_db`); `exceptions.py` (базовый `DomainError` + handler → единый формат ошибок из раздела 6); middleware request_id + structlog JSON-логи; инициализация Alembic (async template); `core/redis.py`, `core/storage.py` (S3-клиент с presigned URL, работает с minio).
**DoD:** пустая миграция применяется; лог запроса содержит request_id; тест `test_health` зелёный; ruff/mypy проходят.

### Задача 0.3 — CI и тестовый контур
**Сделать:** GitHub Actions (или аналог): job'ы lint (ruff, eslint, tsc), test-backend (поднимает postgres+redis сервисами, pytest), build (сборка обоих образов). `tests/conftest.py`: тестовая БД с транзакционным откатом на каждый тест, фабрики через `factory_boy` (заготовка).
**DoD:** CI зелёный на PR; падение любого теста валит pipeline.

---

## Фаза 1. Данные и авторизация (1.1 → 1.2, последовательно)

### Задача 1.1 — Модели БД и миграции
**Контекст из ТЗ:** раздел 4 полностью, 11.1 (поле external_id).
**Сделать:** все SQLAlchemy-модели (users, refresh_tokens, addresses, categories, products, product_variants, product_images, orders, order_items, order_status_history, payments, payment_events, promo_codes) с ограничениями, индексами, search_vector (GENERATED + GIN), pg_trgm-расширение; единая миграция; фабрики для тестов на каждую модель; seed-скрипт `make seed` (3 категории, 10 товаров HobbyLife с вариантами — контейнеры 0.5/1/1.5 л, цвета).
**DoD:** миграция up/down с нуля; CHECK-ограничения реально срабатывают (тесты на невалидные данные); seed наполняет БД.

### Задача 1.2 — Auth-модуль
**Контекст из ТЗ:** 5.5, 6.2, 2.1 (JWT, argon2).
**Сделать:** все эндпоинты 6.2; `core/security.py`; dependency `get_current_user`, `require_role`; ротация refresh с отзывом; rate limiting на Redis (лимиты из 5.5); email-задачи в arq (верификация, сброс пароля) — SMTP из ENV, шаблоны Jinja2; `workers/` + процесс worker в compose.
**DoD:** интеграционные тесты: регистрация → верификация → логин → refresh → logout; повторный refresh старым токеном → 401; rate limit срабатывает; забытый пароль не раскрывает существование email.

---

## Фаза 2. Каталог (2.1 → 2.2 → 2.3; 2.3 можно параллельно с фазой 3)

### Задача 2.1 — API каталога
**Контекст из ТЗ:** 6.1 (categories, products), 5-каталожные правила из 1.2 (опции, атрибуты).
**Сделать:** `GET /categories` (дерево, кэш Redis 5 мин + инвалидация); `GET /products` со всеми фильтрами: категория с подкатегориями, полнотекстовый `q` (tsvector + trgm-fallback для опечаток), price_min/max (по вариантам), `options[...]`, сортировки, пагинация; агрегация доступных фильтров в ответе (`facets`: список опций и диапазон цен по текущей выборке); `GET /products/{slug}`.
**DoD:** тесты на каждый фильтр и их комбинации; поиск «кантейнер» находит «контейнер»; p95 списка на seed-данных < 100 мс локально.

### Задача 2.2 — Админ-CRUD каталога + изображения
**Контекст из ТЗ:** 6.4 (categories, products, variants, images), 2.3 (S3).
**Сделать:** все admin-эндпоинты каталога с `require_role`; загрузка изображений multipart → S3, фоновая задача arq: ресайзы 400/800px webp, запись s3_key; удаление товара — мягкое; валидация: нельзя деактивировать категорию с активными товарами, у товара минимум 1 вариант.
**DoD:** сценарный тест «создать категорию → товар → 2 варианта → загрузить фото → товар виден в публичном каталоге»; превью генерируются.

### Задача 2.3 — Frontend: публичный каталог
**Контекст из ТЗ:** 7.1 (п.1–3), 7.3, 2.2.
**Сделать:** генерация типов из OpenAPI (`npm run gen:api`); layout магазина (Header с поиском и корзиной-счётчиком, Footer); главная (ISR); каталог с фильтрами в URL, skeleton'ы; карточка товара: галерея, выбор варианта, цена/остаток, JSON-LD; форматирование цен `1 250 сом`.
**DoD:** SSR отдаёт готовый HTML с товарами (проверка curl); фильтры шарятся ссылкой; Lighthouse карточки: Perf ≥ 85, SEO ≥ 95; мобильная вёрстка от 360px.

---

## Фаза 3. Корзина и заказы (3.1 → 3.2 → 3.3, последовательно — ядро системы)

### Задача 3.1 — Корзина
**Контекст из ТЗ:** 5.3, 6.1 (cart).
**Сделать:** сервис корзины на Redis (структура, TTL, лимиты из 5.3); cookie `cart_id`; слияние при логине; ответ всегда с актуальными ценами/остатками и флагами `is_available`/`available_qty`; применение промокода к корзине (валидация по 4/promo_codes); эндпоинты 6.1.
**DoD:** тесты: добавление/изменение/удаление; слияние гость+юзер с capping по остаткам; товар деактивирован → в ответе is_available=false; промокод с истёкшим сроком → ошибка с кодом.

### Задача 3.2 — Оформление заказа и конечный автомат
**Контекст из ТЗ:** 5.1, 5.2, 5.4 (способы оплаты), 6.1 (orders), 6.3.
**Сделать:** `POST /orders` — транзакция строго по алгоритму 5.2, с ветвлением по payment_method: cash_on_delivery → сразу processing; online → awaiting_payment + expires_at; генерация number; сервис переходов статусов (валидация автомата, history, email-уведомления при paid/shipped/cancelled); фоновая задача-крон: отмена просроченных awaiting_payment с возвратом остатков; эндпоинты кабинета 6.3 (заказы, отмена, адреса, профиль); гостевой `GET /orders/{number}?email=`.
**DoD:** обязательные тесты: конкурентное оформление последнего товара (asyncio.gather двух заказов — проходит ровно один); таймаут возвращает остатки; каждый запрещённый переход статуса → 409; итоги пересчитаны из БД, подсунутая клиентом цена игнорируется; email-задачи ставятся в очередь.

### Задача 3.3 — Frontend: корзина и checkout
**Контекст из ТЗ:** 7.1 (п.4–5), 7.3.
**Сделать:** страница корзины (оптимистичные мутации, промокод, предупреждения об изменениях); checkout: форма (zod), выбор способа доставки (тарифы с бэка) и способа оплаты (cash_on_delivery / online — список доступных с бэка); для online — редирект на payment_url; success/fail-страницы с поллингом статуса; мини-корзина в шапке.
**DoD:** e2e-сценарий (Playwright): каталог → корзина → checkout с оплатой при получении → страница успеха с номером заказа; изменение остатка конкурентом отражается ошибкой 409 с понятным сообщением.

---

## Фаза 4. Платежи, админка, уведомления (4.1 → 4.2 → 4.3; 4.4+ — по мере получения доступов к банкам)

### Задача 4.1 — Платёжный контур + MockPaymentProvider
**Контекст из ТЗ:** 5.4 полностью.
**Сделать:** `payments/providers/base.py` (интерфейс из 5.4); реестр провайдеров из конфига `PAYMENT_PROVIDERS=mock`; MockPaymentProvider: create_payment возвращает URL внутренней страницы `/api/v1/payments/mock/{id}/page` с кнопками «Оплатить»/«Отклонить», нажатие шлёт подписанный webhook самому себе; обработчик webhook по 5.4 (подпись, идемпотентность через payment_events, сверка суммы, перевод в paid фоновой задачей).
**DoD:** тесты: успешная оплата переводит заказ в paid; двойная доставка события обрабатывается один раз; неверная подпись → 400; сумма не сошлась → payment failed + ERROR-лог; refund меняет статусы.

### Задача 4.2 — Админ: заказы и дашборд
**Контекст из ТЗ:** 6.4 (orders, stats), 7.2.
**Сделать:** админ-фронт: guard по роли; таблица заказов с фильтрами; карточка заказа (состав, клиент, история, кнопки только допустимых переходов, кнопка refund для admin); дашборд `/admin` (summary-эндпоинт: выручка/заказы 7/30 дней, топ-5 товаров); таблицы товаров с inline-редактированием цены/остатка.
**DoD:** менеджер проводит заказ pending→…→delivered из UI; попытка недопустимого перехода невозможна (кнопки нет) и отбивается бэком; статистика совпадает с данными seed-заказов в тесте.

### Задача 4.3 — Telegram-бот для админов
**Контекст из ТЗ:** раздел 5.6 полностью, 4 (`users.telegram_chat_id`), 3.1 (`app/telegram/`), 6.1 (`/webhooks/telegram`), 6.4 (`/admin/telegram/link`). Зависит от 3.2 (`OrderService.transition_status`) и 4.2 (админ-фронт, куда добавляется кнопка привязки) — выдавать агенту после них.

**Сделать:**
1. Миграция: колонка `users.telegram_chat_id text NULL UNIQUE`.
2. `app/telegram/bot_api.py` — тонкая httpx-обёртка над Telegram Bot API: `send_message`, `edit_message_text`, `answer_callback_query`, `set_webhook` (вызывается один раз при старте/деплое, URL из `PUBLIC_BASE_URL`).
3. `app/telegram/service.py`:
   - `create_link_token(user_id) -> url` — одноразовый токен в Redis (TTL 10 мин) + ссылка `t.me/{bot_username}?start={token}`;
   - `handle_start(token, chat_id)` — привязка `telegram_chat_id` к пользователю, удаление токена;
   - `notify_new_order(order)`, `notify_status_change(order)`, `notify_low_stock(variant)`, `send_daily_digest()` — вызываются из существующих точек `OrderService`/фоновых задач, без дублирования бизнес-логики;
   - `handle_callback(update)` — весь алгоритм из раздела 5.6 (секрет, идемпотентность по `update_id`, атрибуция по `chat_id`, вызов `OrderService.transition_status`, редактирование сообщения).
4. `app/telegram/router.py` — `POST /webhooks/telegram` (без auth-зависимости, но с проверкой заголовка секрета внутри).
5. Admin-эндпоинт `POST /admin/telegram/link` (`require_role(manager)`).
6. Фронт: кнопка «Подключить Telegram» в профиле админа/менеджера, показывающая ссылку/QR; статус привязки (подключено/нет).
7. Крон-задача ежедневного дайджеста (arq cron, 20:00 Asia/Bishkek).
8. `.env.example`: `TELEGRAM_BOT_TOKEN`, `TELEGRAM_WEBHOOK_SECRET`, `TELEGRAM_LOW_STOCK_THRESHOLD` (default 5).

**DoD:**
- Тесты сервисного слоя: привязка по валидному/просроченному/несуществующему токену; повторный `update_id` не выполняет действие дважды; callback от непривязанного `chat_id` не меняет заказ; callback с недопустимым переходом статуса не меняет заказ и не роняет обработчик; успешный callback вызывает тот же `OrderService.transition_status`, что и REST-эндпоинт (один тест на оба пути даёт одинаковый результат).
- Ручная проверка (агент фиксирует в описании PR, т.к. требует реального Telegram-аккаунта): привязка по ссылке, получение уведомления о новом заказе, смена статуса кнопкой, редактирование сообщения, дайджест.
- Уведомления не уходят пользователям с ролью `customer`.

### Задачи 4.4 / 4.5 / 4.6 — MBank, ELQR, карточный агрегатор (по факту договоров)
**Шаблон задачи:** реализовать `{Provider}PaymentProvider` по официальной документации (приложить к промпту!), включая генерацию/проверку подписи, маппинг статусов, sandbox-тесты; для ELQR — режим QR (страница оплаты показывает QR и поллит статус). Конфиг-флаг включения. Ничего в общем контуре не менять.
**DoD:** end-to-end в sandbox провайдера; вебхуки идемпотентны; сверка сумм.
**Важно агенту:** если документация не передана — остановиться и запросить, не реконструировать API по догадкам.

---

## Фаза 5. Импорт и запуск (5.1 → 5.2 → 5.3)

### Задача 5.1 — Excel-импорт
**Контекст из ТЗ:** раздел 11.1, 6.4 (imports).
**Сделать:** `app/imports/` с интерфейсом ImportSource + XlsxImportSource (openpyxl); эндпоинты загрузки/предпросмотра/применения/статуса/шаблона; фоновая задача применения: upsert по external_id/SKU, скачивание фото по URL в S3, отчёт об ошибках xlsx; UI в админке (загрузка, предпросмотр 20 строк, прогресс, отчёт).
**DoD:** тестовый файл на 200 строк (сгенерировать в тесте) импортируется; повторный импорт с изменёнными ценами обновляет их и не дублирует товары; битые строки попадают в отчёт, не ломая импорт.

### Задача 5.2 — Промокоды в админке + полировка
**Контекст из ТЗ:** 6.4 (promo-codes), 8.
**Сделать:** CRUD промокодов в админке; сквозной проход по 8: rate limiting на публичных мутациях, security-заголовки, CORS, аудит логов; страницы 404/500 фронта; sitemap.xml + robots.txt; favicon/OG-заглушки HobbyLife.
**DoD:** чек-лист раздела 8 закрыт построчно в описании PR.

### Задача 5.3 — Подготовка к продакшену
**Контекст из ТЗ:** 2.3, 8 (бэкапы).
**Сделать:** prod-вариант compose (без minio — реальный S3; ресурсы/рестарты; Caddy с доменом из ENV); скрипт бэкапа pg_dump + ротация 14 дней (cron-контейнер); nагрузочный smoke (locust/k6: 100 RPS на каталог 2 мин, без 5xx); документация деплоя `docs/DEPLOY.md` (шаги от чистого VPS до работающего магазина).
**DoD:** развёртывание по DEPLOY.md на чистой VM проходит без правок документации; smoke зелёный.

---

## Фаза 6. Закрытие пробелов после 5.3

По итогам независимого аудита от 2026-08-13 (см. `docs/AUDIT-2026-08-13.md`, если он сохранён в репозитории). Задачи-блокеры деплоя (B.1–B.5) и часть важных находок (I.1–I.5) из отчёта аудита выданы агенту отдельно, в готовом карточном формате прямо из самого отчёта — дублировать их здесь не нужно. Ниже — то, что относится к этому плану напрямую.

**Для истории:** после формальной сдачи задачи 5.3 в репозиторий вошли ещё 4 итерации, не оформленные как отдельные задачи плана — личный кабинет + `/admin/users`, админ-панель каталога, повышение тестового покрытия, валидация вложенности категорий. Функционально это те же требования, что уже были в ТЗ (разделы 6.3, 6.4, 4, 9) — план просто не дробил их на отдельные карточки заранее.

### Задача R.1 — Сверить реализацию отзывов и баннеров с формализованной спецификацией
**Контекст из ТЗ:** 5.7, 4 (`product_reviews`, `banners`, поля `rating_avg`/`rating_count`), 6.1/6.3/6.4, 7.1/7.2 — разделы добавлены задним числом после того, как независимый аудит обнаружил в коде рабочую функциональность отзывов и баннеров, отсутствовавшую в ТЗ и плане. Раздел 5.7 прямо помечен как реконструкция ожидаемого поведения, а не спецификация, снятая с уже написанного кода — эта задача должна закрыть разрыв в любую сторону: либо привести код к описанному поведению, либо (если реализация осознанно отличается и это тоже правильно) скорректировать сам раздел 5.7 под факт, явно зафиксировав что и почему изменилось.

**Сделать:** построчно сверить фактический код модулей отзывов/баннеров с разделом 5.7, обращая особое внимание на: (а) действительно ли отзыв разрешён только при наличии `delivered`-заказа с этим товаром — это самая частая недостача в быстрых реализациях подобной фичи; (б) действительно ли `pending`/`rejected`-отзывы скрыты из публичного `GET /products/{slug}/reviews`; (в) пересчитываются ли `rating_avg`/`rating_count` в сервисе при каждой модерации, а не хранятся отдельной, потенциально рассинхронизированной логикой.

**DoD:** тест — покупатель без доставленного заказа на товар не может оставить отзыв (403/409, не 201); тест — `pending`-отзыв не возвращается в публичном списке отзывов; тест — одобрение/отклонение отзыва администратором меняет `rating_avg`/`rating_count` товара корректно (проверить на 2–3 отзывах с разным рейтингом, не только на факте изменения числа); после сверки — раздел 5.7 ТЗ и код не противоречат друг другу ни в одном пункте (аудит уже один раз поймал ровно такое расхождение документации и кода на другой фиче — не повторять его здесь).

---

## Порядок и параллельность

```
0.1 → 0.2 → 0.3 → 1.1 → 1.2 → 2.1 → 2.2 → 3.1 → 3.2 → 3.3 → 4.1 → 4.2 → 4.3 → 5.1 → 5.2 → 5.3
                              └→ 2.3 (параллельно с 3.1–3.2 после 2.1)
4.4–4.6 (реальные платёжные провайдеры) — в любой момент после 4.1, по мере получения доступов от банков
```

Критический путь — фаза 3 (корзина/заказы): её задачи не дробить между агентами и не запускать параллельно.

## Что нужно от заказчика по ходу (не блокирует старт)

1. Подать заявки на эквайринг (MBank + агрегатор) **сразу** — договоры занимают недели, разработка на mock их не ждёт.
2. Логотип, цвета бренда, адрес самовывоза, тарифы доставки — к фазе 2.3.
3. Excel с реальным ассортиментом — к задаче 5.1 (формат шаблона появится после неё, но черновик списка товаров можно готовить уже сейчас).
4. SMTP-доступы (или домен для почты) — к задаче 1.2 достаточно тестового ящика.
5. Домен и VPS — к задаче 5.3.
6. Создать Telegram-бота через @BotFather (пара минут, делаете сами) и передать токен — к задаче 4.3. Публичный домен для webhook нужен тот же, что и для остального API (задача 5.3), поэтому в деле разработки бота до покупки домена можно тестировать через временный туннель (ngrok/cloudflared) — уточните агенту при выдаче задачи, если домен ещё не готов.