# Скопируйте этот файл в .env и при необходимости отредактируйте.
# docker compose читает .env из корня проекта автоматически.

# --- Общее ---
ENVIRONMENT=development

# --- PostgreSQL ---
POSTGRES_USER=hobbylife
POSTGRES_PASSWORD=hobbylife
POSTGRES_DB=hobbylife
POSTGRES_PORT=5432
DATABASE_URL=postgresql+asyncpg://hobbylife:hobbylife@postgres:5432/hobbylife

# --- Redis ---
REDIS_PORT=6379
REDIS_URL=redis://redis:6379/0

# --- S3 / MinIO ---
# Локальная разработка (docker-compose.yml поднимает MinIO как S3-совместимое хранилище):
MINIO_ROOT_USER=hobbylife
MINIO_ROOT_PASSWORD=hobbylife123
MINIO_PORT=9000
MINIO_CONSOLE_PORT=9001
S3_ENDPOINT_URL=http://minio:9000
S3_BUCKET=hobbylife
S3_ACCESS_KEY=hobbylife
S3_SECRET_KEY=hobbylife123
S3_PUBLIC_URL=http://localhost:9000/hobbylife
# Прод (docker-compose.prod.yml без MinIO, см. docs/DEPLOY.md) — например, Cloudflare R2.
# R2 -> API tokens создаются на странице R2 (Account Details -> Manage рядом
# с API Tokens), НЕ через общий "Create Custom Token" в профиле аккаунта.
# S3_PUBLIC_URL для R2 -- это R2.dev/custom-domain URL самого бакета БЕЗ имени
# бакета в пути (в отличие от MinIO выше): R2.dev-домен и кастомные домены
# всегда указывают на один конкретный бакет, а не на аккаунт целиком, и отдают
# объекты как обычный публичный GET без проверки подписи.
# S3_ENDPOINT_URL=https://<account_id>.r2.cloudflarestorage.com
# S3_BUCKET=hobbylife
# S3_ACCESS_KEY=<R2 access key id>
# S3_SECRET_KEY=<R2 secret access key>
# S3_PUBLIC_URL=https://pub-<hash>.r2.dev

# --- Caddy / домен (заполняется перед прод-деплоем, см. Задачу 5.3 и docs/DEPLOY.md) ---
DOMAIN=localhost
# Контактный email для Let's Encrypt (используется только Caddyfile.prod, не влияет на dev).
ACME_EMAIL=admin@example.com

# --- Бэкапы БД (используется начиная с Задачи 5.3, только в docker-compose.prod.yml) ---
BACKUP_RETENTION_DAYS=14

# --- Auth: JWT (используется начиная с Задачи 1.2) ---
JWT_SECRET_KEY=change-me-in-production-use-a-long-random-value-at-least-32-bytes
JWT_ACCESS_TOKEN_TTL_MINUTES=15
JWT_REFRESH_TOKEN_TTL_DAYS=30

# --- Email / SMTP (используется начиная с Задачи 1.2) ---
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASSWORD=
SMTP_FROM=no-reply@hobbylife.kg

# --- CORS (используется начиная с Задачи 0.2) ---
CORS_ORIGINS=http://localhost

# --- Платежи (используется начиная с Задачи 4.1) ---
PAYMENT_PROVIDERS=mock
PAYMENT_WEBHOOK_SECRET=change-me-in-production

# --- Доставка по Бишкеку (используется начиная с Задачи 3.2) ---
COURIER_DELIVERY_COST=150
FREE_DELIVERY_THRESHOLD=3000

# --- Telegram-бот для админов (используется начиная с Задачи 4.3) ---
# Токен и username бота — от @BotFather (username без "@", как в t.me/<username>).
TELEGRAM_BOT_TOKEN=
TELEGRAM_BOT_USERNAME=
# Сверяется с заголовком X-Telegram-Bot-Api-Secret-Token на каждом webhook-запросе.
TELEGRAM_WEBHOOK_SECRET=change-me-in-production
# Порог остатка варианта, ниже которого шлётся алерт (шт.).
TELEGRAM_LOW_STOCK_THRESHOLD=5
# Публичный HTTPS-адрес этого API (без завершающего "/"), на который Telegram
# будет слать webhook — используется только `make telegram-webhook` (см.
# app/scripts/set_telegram_webhook.py), не на каждом старте приложения.
# Прод: https://<ваш домен>/api. Для разработки без домена — временный туннель
# (ngrok/cloudflared) на локальный Caddy.
PUBLIC_BASE_URL=
